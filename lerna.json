{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "version": "2.5.5",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish",
      "registry": "http://registry.npmjs.org/"
    }
  }
}
