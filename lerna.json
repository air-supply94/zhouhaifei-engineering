{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "version": "2.21.22",
  "command": {
    "publish": {
      "verifyAccess": true,
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish",
      "registry": "https://registry.npmjs.org/"
    }
  }
}
