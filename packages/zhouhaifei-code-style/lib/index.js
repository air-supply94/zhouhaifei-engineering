const react = require('./react');
const prettier = require('./prettier');
const stylelint = require('./stylelint');
const commitlint = require('./commitlint');
const eslintNormal = require('./eslintNormal');

module.exports = {
  stylelint,
  prettier,
  react,
  commitlint,
  eslintNormal,
};
